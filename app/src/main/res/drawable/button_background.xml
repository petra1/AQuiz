<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Green Schema (active) -->
    <item android:state_pressed="false">
        <shape android:shape="rectangle">
            <gradient
                android:startColor="@color/gradient_start_green"
                android:centerColor="@color/gradient_center_green"
                android:endColor="@color/gradient_end_green"
                android:angle="270" />
            <corners android:radius="8dp" />
            <stroke android:width="1dp" android:color="@color/white" />
        </shape>
    </item>
    <item android:state_pressed="true">
        <shape android:shape="rectangle">
            <solid android:color="@color/gradient_center_green"/>
            <corners android:radius="8dp" />
            <stroke android:width="1dp" android:color="@color/white" />
        </shape>
    </item>

    <!-- Blue Schema (commented out) -->
    <!--
    <item android:state_pressed="false">
        <shape android:shape="rectangle">
            <gradient
                android:startColor="@color/gradient_start_blue"
                android:centerColor="@color/gradient_center_blue"
                android:endColor="@color/gradient_end_blue"
                android:angle="270" />
            <corners android:radius="8dp" />
            <stroke android:width="1dp" android:color="@color/white" />
        </shape>
    </item>
    <item android:state_pressed="true">
        <shape android:shape="rectangle">
            <solid android:color="@color/gradient_center_blue"/>
            <corners android:radius="8dp" />
            <stroke android:width="1dp" android:color="@color/white" />
        </shape>
    </item>
    -->

    <!-- Purple Schema (commented out) -->
    <!--
    <item android:state_pressed="false">
        <shape android:shape="rectangle">
            <gradient
                android:startColor="@color/gradient_start_purple"
                android:centerColor="@color/gradient_center_purple"
                android:endColor="@color/gradient_end_purple"
                android:angle="270" />
            <corners android:radius="8dp" />
            <stroke android:width="1dp" android:color="@color/white" />
        </shape>
    </item>
    <item android:state_pressed="true">
        <shape android:shape="rectangle">
            <solid android:color="@color/gradient_center_purple"/>
            <corners android:radius="8dp" />
            <stroke android:width="1dp" android:color="@color/white" />
        </shape>
    </item>
    -->

</selector>